# should-counter

A Rails-based REST API for tracking how often someone uses the word "should."

## Development

Install Ruby 2.2.2.

```
bundle install
bundle exec rails s
```

### Testing

```
bundle exec rake
```
